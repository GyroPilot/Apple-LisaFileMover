LISAFile -- Lisa Pascal sources (tool 419311)

Nothing to build yet. When REV A lands:

sources/LFU-MAIN.TEXT      the program
sources/LFU-GLOBALS.TEXT   unit globals (menu constants, panes)
sources/LFU-ALERTS.TEXT    alerts + menus (Alert tool input; menus match the
                           mi* constants in GLOBALS BY POSITION)
sources/LFU-MAKE.TEXT      the Workshop exec: compile, link, Alert tool,
                           InstallTool (tool 419311, name LISAFile)
sources/fmt_text.py        REQUIRED for building: converts each source to the
                           Workshop paged .TEXT format (1024-byte header page,
                           no line straddling a 1024 boundary, bare CR line
                           endings). Raw text files will not compile.
sources/fix_dc42.py        recomputes a DiskCopy 4.2 image's header checksums

Build (same as LISACom): send the formatted files to their LFU/ names on the
Lisa, then from the Workshop: R <LFU/MAKE. Afterwards L LFU/= must show a NEW
LFU.OBJ -- the exec does not stop on a link error.
